# Continue here (alpha review — PC switch)

## Project
Vulkan PBR engine, Alpha v0.1.0. Single unified Scene (scene_unified); old scene.h / SceneNew / RenderListBuilder deleted.

## Build (use project scripts only — run before continuing)
  scripts/linux/build.sh --debug   # or scripts/windows/build.bat --debug
  scripts/linux/build.sh --release # or scripts/windows/build.bat --release
  scripts/linux/clean.sh            # or scripts/windows/clean.bat
  ./install/Debug/bin/VulkanApp levels/default/level.json

## Key files
  ALPHA_REVIEW_CHECKLIST.txt   — full checklist (all sections completed)
  alpha_review/progress.txt    — what's done, leftovers
  alpha_review/questions.txt  — open decisions, Q10 shader tasks

## Done so far
  - All checklist sections 1–13 completed (architecture, usage, efficiency, Vulkan correctness, shader layout, code correctness, guidelines, dead code, build, docs, cursor rule, improvements, scalability).
  - Deprecated 112-byte push helpers removed from object.h; scene_manager uses kInstancedPushConstantSize.
  - descriptor_bindings.h added; coding guidelines (error handling, forward decls); Release build warnings fixed.
  - run_alpha scripts; manager init order in architecture.md.

## Optional / follow-up
  - alpha_review/questions.txt Section 10: all tasks done (global UBO, descriptor writes 1,6,7,8, time_demo.vert/frag + cube in editor viewport).
  - Large commented-out blocks: incremental cleanup when touching files (remove or #if 0).
  - Full coding guidelines compliance (naming, no `!`, literals) applied incrementally to new code.

## If in doubt
  Ask the user. Use only project scripts for build/clean/setup.
